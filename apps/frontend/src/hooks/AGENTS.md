# Hooks Directory

## Description

Custom React hooks for the frontend application. Currently empty - hooks are defined inline in components.

## Key Concepts

- **Future Location**: Place reusable custom hooks here
- **Naming**: Use `use` prefix (e.g., `useDebounce.ts`)
- **TanStack Query**: Query hooks are in `config/api.ts`
