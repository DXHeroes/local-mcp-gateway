# =============================================================================
# CENTRALIZED ENVIRONMENT CONFIGURATION
# =============================================================================
# This is the single source of truth for environment variables in the monorepo.
# All apps and packages load their configuration from this file via Turborepo.
#
# USAGE:
#   1. Copy this file to .env in the root directory: cp .env.example .env
#   2. Update values according to your environment
#   3. Generate BETTER_AUTH_SECRET: openssl rand -hex 32
#   4. Run: pnpm dev (Turborepo automatically loads .env)
#
# SECURITY:
#   - Never commit .env files to git
#   - Keep secrets in .env, not in code
#   - Use separate .env files for different environments
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------

# Environment
NODE_ENV=development
PORT=3001

# Database
# Optional: Path to SQLite database file (defaults to ~/.local-mcp-data/local-mcp.db)
# DATABASE_PATH=

# CORS
# Optional: Comma-separated list of allowed origins
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging
# Optional: Log level (error, warn, info, debug)
# LOG_LEVEL=info

# -----------------------------------------------------------------------------
# AUTHENTICATION (Better Auth)
# -----------------------------------------------------------------------------

# REQUIRED: Secret for Better Auth (min 32 characters) - generate with: openssl rand -hex 32
BETTER_AUTH_SECRET=your-secret-key-min-32-chars-generate-with-openssl-rand-hex-32

# Optional: Base URL for Better Auth
# BETTER_AUTH_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# EMAIL (Resend)
# -----------------------------------------------------------------------------

# Optional: Resend API key - get free key at https://resend.com (100 emails/day)
# If not set, emails will be logged to console in development mode
# RESEND_API_KEY=

# Optional: Email sender (can be "Name <email@example.com>" or just email)
# EMAIL_FROM=Local MCP Gateway <noreply@local-mcp.dev>

# -----------------------------------------------------------------------------
# OAUTH PROVIDERS
# -----------------------------------------------------------------------------

# Optional: Google OAuth credentials
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Optional: GitHub OAuth credentials
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# Optional: OAuth encryption key (min 32 characters)
# OAUTH_ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# MONETIZATION (Paddle)
# -----------------------------------------------------------------------------

# Optional: Paddle API credentials
# PADDLE_API_KEY=
# PADDLE_WEBHOOK_SECRET=
# PADDLE_ENVIRONMENT=sandbox

# -----------------------------------------------------------------------------
# LICENSE KEY SIGNING
# -----------------------------------------------------------------------------

# Optional: Ed25519 keys for license key generation (64 hex chars each)
# Generate with:
#   openssl genpkey -algorithm ed25519 -out private.pem
#   openssl pkey -in private.pem -pubout -out public.pem
#   then convert to hex (64 chars)
# LICENSE_PRIVATE_KEY=
# LICENSE_PUBLIC_KEY=

# -----------------------------------------------------------------------------
# MONITORING
# -----------------------------------------------------------------------------

# Optional: Sentry DSN for error tracking
# SENTRY_DSN=

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION
# -----------------------------------------------------------------------------

# API URL
# IMPORTANT: Must match backend PORT above
# Format: http://localhost:{PORT}
VITE_API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# DOCKER CONFIGURATION (for reference only)
# -----------------------------------------------------------------------------
# When running in Docker, use .env.docker or override in docker-compose.yml:
#
# Docker backend URL (internal network):
# VITE_API_URL=http://backend:3001
#
# Docker database path:
# DATABASE_PATH=/app/data/local-mcp-gateway.db
